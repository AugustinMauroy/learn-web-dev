const { mergeDefault } = require('discord.js');
const fs = require('fs');
const http = require('http');
const MarkdownIt = require('Markdown-It');

function test_md (file){
    return fs.readFileSync(file, function(error, data) {
        if (error){
            console.log(error)
            return error
        }else{
            console.log(data)
            return data
        }
    })  
};

MarkdownIt.Parserblock();

/*affichage*/
http.createServer(function (req, res) {
    res.writeHead(200, {'Content-Type': 'text/html'});
    res.end("hello");
  }).listen(8080);